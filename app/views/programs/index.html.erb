
	<h1>Programs</h1> 
	<div class="column_title pages">
		<%= content_tag :p, link_to("+ New", new_program_path), class: "new_instance_button" %>
		<ul>
			<li class="title">Name</li>
		</ul>
		
	</div>

	<ul class="model_table">
		<% @programs.each do |program| %>
			<li>
				<ul class="instance_detail">
					<%= content_tag :li, program.name, class:"name" %>
					
					<%= render "shared/crud_btns", object: program %>
				</ul>
				<br class="clear">
			</li>
		<% end %>
	</ul>