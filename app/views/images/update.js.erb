 
if (<%= @boolean %> == 0) {
 	$('#edit_image_<%=@image.id%>').parent().parent().parent().fadeOut(3000);
} else if (<%= @boolean %> == 1) {
	// /weekly_scrapbook
	current_scrapbook = window.location.pathname;
    current = current_scrapbook.split("_")[0].slice(1, -2)
    
 	$('#edit_image_<%=@image.id%>').parent(".gallery_msg").html('Image added to your <%= @range %>' );
 	

	
}
