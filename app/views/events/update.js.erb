$("#published_events").html("<%= j render @published_events %>").prepend("<h3>Published Events (<%= Event.published.count %>)")
$("#scheduled_events").html("<%= j render @scheduled_events %>").prepend("<h3>Scheduled Events (<%= Event.scheduled.count %>)")
$("#draft_events").html("<%= j render @draft_events %>").prepend("<h3>Draft Events (<%= Event.draft.count %>)")